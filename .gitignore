# Internal business notes — not published to the public repo/site.
# Whole directory ignored (not individual filenames) so any file saved in
# here, under any name or extension, is excluded automatically.
private/

.claude/
.DS_Store

node_modules/
dist/
*.local
